pipeline {
    agent {
        kubernetes {
            yamlFile 'pod.yaml'  // Reference the pod.yaml stored in the GitHub repository
        }
    }

    environment {
        DOCKER_IMAGE = "daudidrees/aks-k8s-rollouts"
        APP_NAME = "my-jenkins-rollback-deployments"
        ARGOCD_USERNAME = 'admin'
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
        KUBE_TOKEN = credentials('my_kubernetes')  // K8s token for authentication
        KUBE_SERVER = "$(kubectl config view --minify -o jsonpath='{.clusters[0].cluster.server}')" // Get Kubernetes API server
        NAMESPACE = "nodejs"
    }

    stages {
        stage('Checkout') {
            steps {
                script {
                    git url: 'https://github.com/Daudkhan1/aks-k8s-rollouts.git', branch: 'main'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                container('docker') {
                    sh "docker build -t ${DOCKER_IMAGE}:${BUILD_NUMBER} ."
                }
            }
        }

        stage('Trivy Scan') {
            steps {
                container('trivy') {
                    sh "trivy image --exit-code 0 --severity HIGH,CRITICAL ${DOCKER_IMAGE}:${BUILD_NUMBER}"
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                container('docker') {
                    withDockerRegistry([credentialsId: 'dockerhub-credentials', url: '']) {
                        sh "docker push ${DOCKER_IMAGE}:${BUILD_NUMBER}"
                    }
                }
            }
        }

        stage('Update Kubernetes Manifests') {
            steps {
                script {
                    sh "sed -i 's|newTag: .*|newTag: \"${BUILD_NUMBER}\"|' menifests/kustomization.yaml"
                }
            }
        }

        stage('Apply Rollout') {
            steps {
                script {
                    sh """
                    kubectl --token=${KUBE_TOKEN} --server=${KUBE_SERVER} apply -k menifests/
                    kubectl --token=${KUBE_TOKEN} --server=${KUBE_SERVER} rollout status deployment/nodejs-app -n ${NAMESPACE} || true
                    """
                }
            }
        }

        stage('Rollback on Failure') {
            when {
                expression {
                    def rollout_status = sh(script: """
                        kubectl --token=${KUBE_TOKEN} --server=${KUBE_SERVER} get deployment nodejs-app -n ${NAMESPACE} -o jsonpath='{.status.conditions[?(@.type=="Progressing")].reason}'
                    """, returnStdout: true).trim()
                    return rollout_status == "ProgressDeadlineExceeded"
                }
            }
            steps {
                script {
                    echo "🚨 Deployment exceeded progress deadline! Rolling back..."
                    sh "kubectl --token=${KUBE_TOKEN} --server=${KUBE_SERVER} rollout undo deployment nodejs-app -n ${NAMESPACE} --to-revision=0"
                    echo "🔥 Force deleting stuck pods..."
                    sh "kubectl --token=${KUBE_TOKEN} --server=${KUBE_SERVER} delete pod -l app=nodejs-app -n ${NAMESPACE} --force --grace-period=0"
                    echo "🔄 Rollback completed!"
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
        success {
            echo '✅ Deployment completed successfully!'
        }
        failure {
            echo '❌ Deployment failed!'
        }
    }
}
