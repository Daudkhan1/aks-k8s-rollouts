apiVersion: v1
kind: Service
metadata:
  name: svc-active
  namespace: nodejs
  labels:
    app: nodejs
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 1337
      nodePort: 30001
  selector:
    app: nodejs
